{
  "version": 2,
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "web-build",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}